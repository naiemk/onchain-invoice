# Trustless Commerce product templates

Opaque config and start/update scripts for the [infra packager](../../infra/README.md).

Fetched by `infra/install.sh` using `rawBase` from [`packageconfig.yaml`](../packageconfig.yaml).

Do not put packager logic here — only product-specific YAML, `.env` examples, nginx snippets, and lifecycle scripts.
