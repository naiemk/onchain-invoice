serverUrl: "${SERVER_URL}"
sweeperWalletKey: "${SWEEPER_WALLET_KEY}"
apiKey: "${SWEEPER_API_KEY}"
activityLogPath: "${ACTIVITY_LOG_PATH}"
intervalMs: 15000
maxRetries: 5
# Prefer SWEEPER_ROLE=solana via compose
chains: []
tron:
  enabled: false
solana:
  enabled: true
  privateKey: "${SOLANA_SWEEPER_KEY}"
  feeRecipient: "${SOLANA_FEE_RECIPIENT}"
  feeBps: 50
  # Same settle path for every chainId — enable mainnet-beta when program is deployed.
  chains:
    devnet:
      enabled: true
      rpcUrl: "${SOLANA_RPC_URL}"
      programId: "${SOLANA_PROGRAM_ID}"
      tokens:
        USDC:
          mint: "${SOLANA_USDC_MINT}"
          decimals: 6
        USDT:
          mint: "${SOLANA_USDT_MINT}"
          decimals: 6
    mainnet-beta:
      enabled: false
      rpcUrl: "${SOLANA_MAINNET_RPC_URL}"
      programId: "${SOLANA_MAINNET_PROGRAM_ID}"
      tokens:
        USDC:
          mint: EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
          decimals: 6
        USDT:
          mint: Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB
          decimals: 6
